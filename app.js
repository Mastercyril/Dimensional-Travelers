// Placeholder for VIVERSE SDK initialization
// Replace this with actual VIVERSE SDK script if available

document.getElementById('login-btn').addEventListener('click', function() {
  // Simulate VIVERSE login process
  document.getElementById('status').textContent = "Logging in with VIVERSE...";

  // Simulate async login (replace this with real SDK call)
  setTimeout(() => {
    document.getElementById('status').textContent = "Logged in! (VIVERSE stub)";
  }, 1500);
});